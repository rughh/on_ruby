cache:
  image: memcached:1.4.22
  ports:
    - "11211:11211"

db:
  image: postgres:9.4.1
  ports:
    - "5432:5432"

web:
  build: .
  command: "script/server"
  ports:
    - "5000:5000"
  links:
    - cache
    - db
  volumes:
    - .:/myapp
