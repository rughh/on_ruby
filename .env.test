# Dummy Google OAuth creds - read once at boot (config/initializers/omniauth.rb),
# so they can't be stubbed per-example.
GOOGLE_CONSUMER_KEY=test_google_client_id
GOOGLE_CONSUMER_SECRET=test_google_client_secret
