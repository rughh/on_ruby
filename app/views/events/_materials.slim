- if materials.present?
  h3.card-title#materials= t("main.materials")
  ul
    - materials.each do |material|
      li
        = link_to_material(material)
        == " (#{link_to_user(material.user)})" if material.user
        = material_preview(material)
