= section_box :usergroups do
  %ul
    - Whitelabel.labels.each do |label|
      %li
        = link_to root_url(subdomain: label.label_id) do
          .whitelabel
            = image_tag("labels/#{label.label_id}.png")
            = label.host
