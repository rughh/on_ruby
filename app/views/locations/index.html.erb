<section>
  <h2><%= title %></h2>
  <%= map(locations) %>
  <br>

  <ul class="more-list enumeration">
    <% locations.each do |location| %>
      <li><%= link_to location.name, location %></li>
    <% end %>
  </ul>
</section>
