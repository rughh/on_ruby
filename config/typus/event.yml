Event:
  fields:
    default: id, name, date, user
    form: user, location, name, date, description
  order_by: -created_at
  search: name, description
  actions:
    edit: publish
  application: CRUD
